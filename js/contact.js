function initContactForm(){const form=document.getElementById('contactForm');if(!form||form.dataset.enhanced==='true')return;form.dataset.enhanced='true';const button=form.querySelector('button[type="submit"]');const status=document.getElementById('contactStatus');form.addEventListener('submit',async event=>{event.preventDefault();const original=button.textContent;button.disabled=true;button.textContent='שולח...';if(status)status.textContent='';try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!response.ok)throw new Error('send-failed');form.reset();if(status)status.textContent='הפנייה נשלחה בהצלחה';if(window.showToast)window.showToast('הפנייה נשלחה בהצלחה');setTimeout(()=>{if(window.closeModal)window.closeModal(document.getElementById('contactModal'));if(status)status.textContent=''},900)}catch(error){if(status)status.textContent='לא הצלחנו לשלוח כרגע. נסו שוב בעוד רגע';if(window.showToast)window.showToast('שליחת הפנייה נכשלה')}finally{button.disabled=false;button.textContent=original}})}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',initContactForm)}else{initContactForm()}