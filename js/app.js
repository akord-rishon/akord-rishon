document.addEventListener('DOMContentLoaded',()=>{loadEnhancements();installLiquidCards();installModals();upgradeContactForm()});function loadEnhancements(){['css/liquid.css','css/forms.css'].forEach(href=>{if(document.querySelector(`link[href="${href}"]`))return;const link=document.createElement('link');link.rel='stylesheet';link.href=href;document.head.appendChild(link)});if(!document.getElementById('liquid-displacement-svg')){const holder=document.createElement('div');holder.id='liquid-displacement-svg';holder.setAttribute('aria-hidden','true');holder.style.cssText='position:absolute;width:0;height:0;overflow:hidden';holder.innerHTML='<svg width="0" height="0"><filter id="liquid-displacement" x="-40%" y="-40%" width="180%" height="180%"><feTurbulence type="fractalNoise" baseFrequency="0.018 0.04" numOctaves="2" seed="7" result="noise"><animate attributeName="baseFrequency" dur="1.2s" values="0.018 0.04;0.032 0.018;0.018 0.04" repeatCount="indefinite"/></feTurbulence><feDisplacementMap in="SourceGraphic" in2="noise" scale="18" xChannelSelector="R" yChannelSelector="B"/></filter></svg>';document.body.prepend(holder)}if(!document.querySelector('script[src="js/contact.js"]')){const script=document.createElement('script');script.src='js/contact.js';script.defer=true;document.body.appendChild(script)}}function installLiquidCards(){document.querySelectorAll('.glass-card').forEach((card,index)=>{card.style.animationDelay=`${Math.min(index*.07,.45)}s`;card.addEventListener('pointermove',event=>{const rect=card.getBoundingClientRect();card.style.setProperty('--x',`${event.clientX-rect.left}px`);card.style.setProperty('--y',`${event.clientY-rect.top}px`)});card.addEventListener('pointerdown',event=>{const rect=card.getBoundingClientRect();const x=event.clientX-rect.left;const y=event.clientY-rect.top;card.style.setProperty('--press-x',`${x}px`);card.style.setProperty('--press-y',`${y}px`);const wave=document.createElement('span');wave.className='liquid-wave';wave.style.setProperty('--press-x',`${x}px`);wave.style.setProperty('--press-y',`${y}px`);card.appendChild(wave);card.classList.remove('liquid-active');void card.offsetWidth;card.classList.add('liquid-active');setTimeout(()=>{wave.remove();card.classList.remove('liquid-active')},840)})})}function installModals(){document.querySelectorAll('[data-open]').forEach(button=>button.addEventListener('click',()=>openModal(button.dataset.open)));document.querySelectorAll('[data-close]').forEach(button=>button.addEventListener('click',()=>closeModal(button.closest('.modal'))));document.querySelectorAll('.modal').forEach(modal=>modal.addEventListener('click',event=>{if(event.target===modal)closeModal(modal)}));document.addEventListener('keydown',event=>{if(event.key==='Escape')document.querySelectorAll('.modal.open').forEach(closeModal)})}function upgradeContactForm(){const form=document.querySelector('#contactModal form');if(!form)return;form.id='contactForm';form.classList.add('contact-form');const fields=[...form.querySelectorAll('.field')];const labels=['שם מלא','אימייל לחזרה','איך אפשר לעזור?'];fields.forEach((field,index)=>{field.placeholder=' ';const wrap=document.createElement('div');wrap.className='field-wrap';field.parentNode.insertBefore(wrap,field);wrap.appendChild(field);const label=document.createElement('label');label.className='field-label';label.textContent=labels[index]||'';wrap.appendChild(label)});if(!document.getElementById('contactStatus')){const status=document.createElement('div');status.id='contactStatus';status.className='contact-status';status.setAttribute('aria-live','polite');form.appendChild(status)}const panel=form.closest('.modal-panel');if(panel)panel.classList.add('contact-panel')}function openModal(id){const modal=document.getElementById(id);if(!modal)return;modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.classList.add('modal-open');const focusable=modal.querySelector('button,input,textarea');if(focusable)setTimeout(()=>focusable.focus(),80)}function closeModal(modal){if(!modal)return;modal.classList.remove('open');modal.setAttribute('aria-hidden','true');if(!document.querySelector('.modal.open'))document.body.classList.remove('modal-open')}function showToast(text){const toast=document.getElementById('toast');if(!toast)return;toast.textContent=text;toast.classList.add('show');clearTimeout(window.__toastTimer);window.__toastTimer=setTimeout(()=>toast.classList.remove('show'),2400)}window.openModal=openModal;window.closeModal=closeModal;window.showToast=showToast;