document.addEventListener('DOMContentLoaded',()=>{const canvas=document.getElementById('gameCanvas');if(!canvas)return;const ctx=canvas.getContext('2d');const scoreEl=document.getElementById('score');const bestEl=document.getElementById('best');const startBtn=document.getElementById('startGame');const shareBtn=document.getElementById('shareGame');const grid=20;let snake,food,dir,nextDir,score,best,loopId,running=false;best=Number(localStorage.getItem('akordBest')||0);bestEl.textContent=best;function reset(){snake=[{x:8,y:8},{x:7,y:8},{x:6,y:8},{x:5,y:8}];food={x:14,y:10};dir={x:1,y:0};nextDir={x:1,y:0};score=0;scoreEl.textContent=score;draw()}function start(){clearInterval(loopId);reset();running=true;startBtn.textContent='התחל מחדש';loopId=setInterval(tick,105)}function tick(){dir=nextDir;const head={x:snake[0].x+dir.x,y:snake[0].y+dir.y};if(head.x<0)head.x=19;if(head.x>19)head.x=0;if(head.y<0)head.y=19;if(head.y>19)head.y=0;if(snake.some(p=>p.x===head.x&&p.y===head.y)){end();return}snake.unshift(head);if(head.x===food.x&&head.y===food.y){score++;scoreEl.textContent=score;if(score>best){best=score;bestEl.textContent=best;localStorage.setItem('akordBest',best)}placeFood()}else snake.pop();draw()}function placeFood(){do{food={x:Math.floor(Math.random()*20),y:Math.floor(Math.random()*20)}}while(snake.some(p=>p.x===food.x&&p.y===food.y))}function draw(){const size=canvas.width/20;ctx.clearRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#050507';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.strokeStyle='rgba(255,255,255,.035)';for(let i=1;i<20;i++){ctx.beginPath();ctx.moveTo(i*size,0);ctx.lineTo(i*size,canvas.height);ctx.stroke();ctx.beginPath();ctx.moveTo(0,i*size);ctx.lineTo(canvas.width,i*size);ctx.stroke()}const grad=ctx.createRadialGradient((food.x+.5)*size,(food.y+.5)*size,2,(food.x+.5)*size,(food.y+.5)*size,size*.8);grad.addColorStop(0,'#ffd978');grad.addColorStop(1,'#ff2947');ctx.fillStyle=grad;ctx.beginPath();ctx.arc((food.x+.5)*size,(food.y+.5)*size,size*.34,0,Math.PI*2);ctx.fill();snake.forEach((p,i)=>{const g=ctx.createLinearGradient(p.x*size,p.y*size,(p.x+1)*size,(p.y+1)*size);g.addColorStop(0,i===0?'#fff':'#ff4961');g.addColorStop(1,i===0?'#f0c86b':'#8d1025');ctx.fillStyle=g;roundRect(ctx,p.x*size+2,p.y*size+2,size-4,size-4,6);ctx.fill()})}function roundRect(c,x,y,w,h,r){c.beginPath();c.roundRect(x,y,w,h,r)}function end(){running=false;clearInterval(loopId);showToast(`המשחק הסתיים · ${score} נקודות`)}function change(x,y){if(x===-dir.x&&y===-dir.y)return;nextDir={x,y}}document.querySelectorAll('[data-dir]').forEach(btn=>btn.addEventListener('click',()=>{const d=btn.dataset.dir;if(d==='up')change(0,-1);if(d==='down')change(0,1);if(d==='left')change(-1,0);if(d==='right')change(1,0)}));document.addEventListener('keydown',e=>{if(!running)return;if(e.key==='ArrowUp')change(0,-1);if(e.key==='ArrowDown')change(0,1);if(e.key==='ArrowLeft')change(-1,0);if(e.key==='ArrowRight')change(1,0)});startBtn.addEventListener('click',start);shareBtn.addEventListener('click',()=>{const text=encodeURIComponent(`השגתי ${score} נקודות באתגר אקורד ראשון. נסו לעקוף אותי: ${location.href}`);window.open(`https://wa.me/?text=${text}`,'_blank','noopener')});reset()});